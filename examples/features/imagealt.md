# Image alt text

Embedded images carry their Markdown alt text as Word alt text (`descr`), so
screen readers and the accessibility checker can describe them.

![A tidy left-to-right flow diagram](../diagrams/01-flow.svg "Flowchart")

An image with only a title falls back to it for the alt text:

![](../diagrams/01-flow.svg "Fallback description")
